# Placeholder OpenFAST primary input for the coupling smoke-case scaffold.
# The current stub backend validates that this file exists but does not parse it.
# Replace this directory with a real OpenFAST input set when implementing the
# production wrapper backend.

"Placeholder OC4 OpenFAST smoke case"
