/*--------------------------------*- C++ -*----------------------------------*\
\*---------------------------------------------------------------------------*/
FoamFile { version 2.0; format ascii; class dictionary; object blockMeshDict; }
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //
// 2D wave tank EXTENDED to 800 m (x[-400,400]) to get a clean multi-wavelength
// field. L=151 m -> 800 m = 5.3 L. Inlet relaxation zone x[-400,-250] (~1 L
// generation), outlet zone x[250,400] (~1 L absorption), leaving x[-250,250]
// (500 m ~= 3.3 L) of clean propagation. z[-50,30] (SWL 0, 50 m water), y thin.
//
// Resolution: x 537 (dx~1.49 m, ~101/L); z 160 (dz=0.5 m, ~12/H); y 1.
// 537*1*160 = 85920 cells.

convertToMeters 1;

vertices
(
    (-400 -2.5 -80)   // 0
    ( 561 -2.5 -80)   // 1
    ( 561  2.5 -80)   // 2
    (-400  2.5 -80)   // 3
    (-400 -2.5  30)   // 4
    ( 561 -2.5  30)   // 5
    ( 561  2.5  30)   // 6
    (-400  2.5  30)   // 7
);

blocks
(
    hex (0 1 2 3 4 5 6 7) (801 1 367) simpleGrading (1 1 1)
);

edges ( );

boundary
(
    inlet    { type patch; faces ( (0 4 7 3) ); }
    outlet   { type patch; faces ( (1 2 6 5) ); }
    bottom   { type wall;  faces ( (0 3 2 1) ); }
    atmosphere { type patch; faces ( (4 5 6 7) ); }
    frontBack { type empty; faces ( (0 1 5 4) (3 7 6 2) ); }
);

mergePatchPairs ( );
// ************************************************************************* //
